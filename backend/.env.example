# Backend environment example - copy to .env and fill the values (DO NOT COMMIT .env)
# Server
PORT=3000
NODE_ENV=development

# Postgres (example local): postgres://user:pass@host:port/dbname
# For local dev you can use: postgres://postgres:postgres@localhost:5432/crypto_trader_dev
DATABASE_URL=

# Binance credentials
# Use BINANCE_MODE=mock during development to avoid real trades
BINANCE_API_KEY=
BINANCE_API_SECRET=
BINANCE_MODE=mock

# Optional: toggle verbose logging
LOG_LEVEL=info
